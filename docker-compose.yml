version: '3.8'
services:
  mysql:
    image: mysql:8.0
    container_name: mysql_bookcut
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: bookcut_db
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: